@using GameOn.Model
@model IList<Product>

@{
    ViewBag.Title = "Home";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Browse</h1>
<ul>
    @foreach (var product in Model)
    {
        <li>@Html.ActionLink(product.Name, "ProductDetails", new{productId=product.Id})</li>
    }
</ul>